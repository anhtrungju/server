execute if score *player_stats Module matches 1 if score *Timer12s Runechant_Timers matches 1.. run tag @s add stat_gain_enchant
execute if score *player_stats Module matches 1 if score *Timer12s Runechant_Timers matches 1.. run tag @s add stat_gain_vigor
